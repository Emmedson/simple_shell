#include "shell.h"
#include <stdio.h>
/**
* main - check the code
* Return: Always 0.
*/
int main(void)
{
	int a, b, c;

	a = 20;
	b = 30;
	c = a + b;
	printf("The sum of a and b is %d/n", c);
	return (0);
}
